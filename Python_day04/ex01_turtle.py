from turtle import Turtle

t = Turtle()
t.pensize(3)
t.pencolor('Red')
t.speed(0)

length = 5
while True < 500 :
    t.left(89)
    t.forward(length)
    length += 3


input('Press any key to continue...')